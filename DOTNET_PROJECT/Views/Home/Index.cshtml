@* 
    Razor view for the game's start screen.
    Displays a simple form where the player can enter their name to begin a new game.
    - Uses StartGameViewModel for validation and binding.
    - Shows any TempData["Error"] messages from the controller (e.g., if no name was entered).
    - On submission, the form posts to GameMvcController.Start to begin the game session.
*@
@model DOTNET_PROJECT.Models.StartGameViewModel
@{
    ViewData["Title"] = "Start";
}
<h2>Start a Game (Frontend Safe Mode)</h2>

@if (TempData["Error"] != null)
{
    <div class="error">@TempData["Error"]</div>
}

<form asp-controller="GameMvc" asp-action="Start" method="post">
    <label for="name">Your name</label>
    <input id="name" name="Name" type="text" value="Test Pilot" />
    <button type="submit">Start</button>
</form>
